spring:
    config:
        import: optional:file:./.env[.properties]

    application:
        name: share

aws:
    s3:
        bucket: hackaton-groupe10
    region: eu-north-1
    credentials:
        access-key: ${AWS_ACCESS_KEY_ID}
        secret-key: ${AWS_SECRET_ACCESS_KEY}


